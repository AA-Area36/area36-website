# Plan: Update Service Page with Real Area 36 Resources

## Summary
Replace the placeholder Area 36 resources on the `/service` page with real resources from Google Drive folder `1O9ec6xM1Z_XXlU3C53-eX6XBDKWA82Nx`. Resources will be displayed grouped by category (similar to how committees display files).

## Categories
Files will be grouped by these categories (set via file metadata in admin):
- General Materials
- General Service Representatives (GSRs)
- Group Servants
- District Committee Member
- Area & District Committee Chairs
- Area Trusted Servants
- Group Inventory

## Files to Create

### 1. `lib/gdrive/service-resources.ts`
Create a new module for fetching service resources from Google Drive:
- Similar structure to `lib/gdrive/committees.ts`
- `getServiceResources(credentials, folderId)` function
- Reuse existing types: `CommitteeFile` interface (or create `ServiceResource` interface)
- Cache results for 5 minutes

### 2. `app/service/actions.ts`
Create server action for fetching service resources:
- Similar to `app/committees/actions.ts`
- `fetchServiceResources()` function
- Use new `GDRIVE_SERVICE_RESOURCES_FOLDER_ID` env var
- Enrich files with metadata (displayName, password, category)

### 3. `app/service/service-resources.tsx`
Create client component for displaying categorized resources:
- Similar to `CommitteeFilesSection` usage in `committees-content.tsx`
- Group files by category using `groupFilesByCategory()` pattern
- Reuse `CommitteeFilesSection` component for consistent file display
- Handle view/download with PDF viewer and password protection

## Files to Modify

### 4. `app/service/page.tsx`
Update the service page:
- Convert to async page to fetch resources on server
- Import and use new `ServiceResources` component
- Replace hardcoded `localResources` array with dynamic data
- Keep GSO Resources section as-is (external links)

### 5. `env.d.ts`
Add new environment variable type:
```typescript
GDRIVE_SERVICE_RESOURCES_FOLDER_ID: string
```

### 6. `app/admin/(dashboard)/files/actions.ts`
Register the new folder in admin file manager:
- Add `GDRIVE_SERVICE_RESOURCES_FOLDER_ID` to `getEnv()`
- Add to `rootFolders` array in `getFolderStructure()`

## Environment Setup (Manual)
Add to `.dev.vars` and Cloudflare environment:
```
GDRIVE_SERVICE_RESOURCES_FOLDER_ID=1O9ec6xM1Z_XXlU3C53-eX6XBDKWA82Nx
```

## Implementation Order
1. Add environment variable types (`env.d.ts`)
2. Create `lib/gdrive/service-resources.ts`
3. Create `app/service/actions.ts`
4. Create `app/service/service-resources.tsx`
5. Update `app/service/page.tsx`
6. Register folder in admin (`app/admin/(dashboard)/files/actions.ts`)

## Verification
1. Add `GDRIVE_SERVICE_RESOURCES_FOLDER_ID` to local `.dev.vars`
2. Run `npm run dev` and navigate to `/service`
3. Verify resources load from Google Drive
4. Test file viewing and downloading
5. Navigate to Admin > Files and verify new folder appears
6. Set categories on files via admin and verify grouping on service page
